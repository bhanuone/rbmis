- breadcrumb :edit_city_area, @city, @area
.ui.centered.grid
  .header
    %h4 Edit area
  .eight.wide.column
    = form_for @area, {url: city_area_path(@city, @area), html: {class: 'ui form'}} do |f|
      = render 'form', f: f